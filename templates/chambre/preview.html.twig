       
    <div class="row">
        <div class="col-md-3 thumbnail text-center">
            <img src="{{ chambre.image }}" alt="chambre bootstrap" class="img-responsive">
            <div class="caption">
                <h3><a href="{{ path('app_chambre_index', {id:chambre.id}) }}">{{ chambre.nom }}</a></h3>
            </div>
            
    </div>